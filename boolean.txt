function main(): void {
	let a: int;
	let b: bool;

	a = 0;
	if (a == 0) {
		log a;
	}

	if ((a != 0) == false) {
		log a;
	}

	b = true;
	if (b) {
		log b;
	}
	if (!b) {
		log b; // No entra
	}

	//if (a) {}
	// Error a no es bool;
}